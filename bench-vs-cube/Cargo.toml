[package]
name = "bench-vs-cube"
version = "0.1.0"
edition = "2021"

[dependencies]
airlayer = { path = ".." }
duckdb = { version = "1", features = ["bundled"] }
serde_json = "1.0"
ureq = { version = "2", features = ["json"] }
